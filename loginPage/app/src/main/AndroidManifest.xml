<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.arvin.loginpage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".welcomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".loginPage" />
        <activity android:name=".signupPage" />
        <activity android:name=".transferPage" />
        <activity android:name=".loginhomePage" />
        <activity android:name=".utilitiesPage" />
        <activity android:name=".checkbalancePage" />
        <activity android:name=".transferhistoryPage" />
        <activity android:name=".accountrecoveryPage"></activity>
    </application>

</manifest>